{
  "name": "pts-cloudflare-portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'Static Cloudflare Pages build: no build step required'",
    "test": "node --test",
    "lint": "node --check functions/_lib/*.js functions/api/*.js"
  },
  "engines": {
    "node": ">=20"
  }
}
